# Exact pinned versions used for the published results were not recorded;
# these are the packages actually imported across the pipeline, with
# practical minimum versions for the APIs used (pm.Model(coords=...),
# the numpyro sampler backend, nullable Int64 dtypes).
numpy>=1.24
pandas>=2.0
pyarrow>=12.0
matplotlib>=3.7
statsmodels>=0.14
scikit-learn>=1.3
pymc>=5.10
numpyro>=0.13
jax>=0.4
arviz>=0.16
openai>=1.0
tqdm>=4.65
